const RPC = require("discord-rpc")

module.exports = async client => {
// const clientId = '837251737630408734';
// const scopes = ['rpc', 'rpc.api', 'messages.read'];

// const clients = new RPC.Client({ transport: 'websocket' });

// clients.on('ready', () => {
//   console.log('Logged in as', clients.application.name);
//   console.log('Authed for user', clients.user.username);

//   clients.selectVoiceChannel('81384788862181376');
// });

// // Log in to RPC with client id
// clients.login({ clientId, scopes });
 
}