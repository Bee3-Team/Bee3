<script>
    	$(window).on("load", function() {
      $(".loading-dot").fadeOut(1500);
      $(".on-loading").removeClass("on-loading")
        console.log("[beee.cf] successfuly load website")
	});
</script>
<style>
.loading-dot {
    display: flex;
    justify-content: center;
    align-items: center !important;
    width: 100%;
    height: 100%;
    position: fixed;
  background-color: #ffd700;
  z-index: 9999;
}

.dot {
  transition: 1s;
  width: 3px;
  height: 35px;
  border-radius: 5px;
  background-color: white;
  margin: 5px;
  box-shadow: 0px 0px 3px 2px rgb(0,0,0,0.5);
}

.dot:nth-last-child(1) {
  background: blue;
  animation: loadingC 1s 0.1s linear infinite;
}
.dot:nth-last-child(2) {
  background: red;
  animation: loadingC 1s 0.2s linear infinite;
}
.dot:nth-last-child(3) {
  background: green;
  animation: loadingC 1s 0.3s linear infinite;
}

@keyframes loadingC {
  0 {
    transform: translate(0, 0);
  }
  50% {
    transform: translate(0, 15px);
  }
  100% {
    transform: translate(0, 0);
  }
}  
</style>
<div class="loading-dot">
  <div class="dot"></div>
  <div class="dot"></div>
  <div class="dot"></div>
</div>